# 2026-03-23

- Added a reusable local-first relay layer in `packages/contex-relay/` for participant tracking, direct/channel messaging, central BCC archive, relationship analysis, and provider-agnostic agent runtime loops.
- Wired relay into Electron main/preload/renderer (`src/main/relay/`, `src/main/ipc/relay.ts`, `src/preload/index.ts`, `src/renderer/src/App.tsx`).
- Mirrored legacy `collab:sendMessage` tile DMs into the relay central archive so workspace-level feeds do not miss existing messages.
- Current runtime executors: Claude + Codex. OpenCode executor still needs implementation in `src/main/relay/provider-executor.ts`.
- Full `npm run build` passed after integration.
